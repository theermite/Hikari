/// <reference types="vite/client" />

/** La version de l'application, injectée à la construction depuis `tauri.conf.json` —
 * le fichier qui décide aussi de la version du paquet installé. */
declare const __APP_VERSION__: string;
